name: CI

on:
  push:
    branches: [main, feat/*, fix/*]
    paths-ignore: ["**.md"]
  pull_request:
    branches: [main]
    paths-ignore: ["**.md"]

jobs:
  ci:
    uses: Nadav011/ci-standards/.github/workflows/ci-nextjs.yml@main
    with:
      package-manager: npm
    secrets: inherit
